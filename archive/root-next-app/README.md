# Archived root Next.js app

This directory contains the deprecated root-level Next.js app that previously lived at `/app`.

The canonical production frontend now lives at `/web/src/app`.

Do not add new routes here.
